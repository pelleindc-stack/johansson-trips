const CACHE_NAME = 'portugal-trip-v5';
const APP_SHELL = ['./', './index.html', './styles.css', './app.js', './auth.js', './manifest.webmanifest', './icon-192.png', './icon-512.png', './photos/tap-outbound.jpg', './photos/flight-note.jpg', './photos/flight-summary.jpg', './photos/tap-search.png', './photos/lisbon-airbnb.jpg', './photos/airbnb-policy.png', './photos/dona-filipa.png', './photos/amsterdam-hotel-confirmation.png', './photos/united-amsterdam-return.png'];
self.addEventListener('install', event => event.waitUntil(caches.open(CACHE_NAME).then(cache => cache.addAll(APP_SHELL)).then(() => self.skipWaiting())));
self.addEventListener('activate', event => event.waitUntil(caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith('portugal-trip-') && key !== CACHE_NAME).map(key => caches.delete(key)))).then(() => self.clients.claim())));
self.addEventListener('fetch', event => { if (event.request.method !== 'GET') return; const url = new URL(event.request.url); if (url.origin !== self.location.origin || !url.pathname.startsWith(new URL('./', self.location.href).pathname)) return; if (event.request.mode === 'navigate') { event.respondWith(fetch(event.request).catch(() => caches.match('./index.html'))); return; } event.respondWith(caches.match(event.request).then(cached => cached || fetch(event.request))); });
