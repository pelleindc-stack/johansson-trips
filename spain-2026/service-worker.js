const CACHE_NAME = 'spain-trip-v6';
const APP_SHELL = ['./', './index.html', './styles.css', './app.js', './auth.js', './manifest.webmanifest', './icon-192.png', './icon-512.png', './route-map.svg', './photos/united-confirmation.jpg', './photos/scheduled-1.jpg', './photos/scheduled-2.jpg', './photos/olivia-chloe-return.png', './photos/royal-palm-22.png', './tickets/caminito-per-johansson.pdf', './tickets/caminito-renee-johansson.pdf', './tickets/caminito-lukas-johansson.pdf', './tickets/caminito-chloe-johansson.pdf', './tickets/caminito-olivia-averill.pdf'];
self.addEventListener('install', event => event.waitUntil(caches.open(CACHE_NAME).then(cache => cache.addAll(APP_SHELL)).then(() => self.skipWaiting())));
self.addEventListener('activate', event => event.waitUntil(caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith('spain-trip-') && key !== CACHE_NAME).map(key => caches.delete(key)))).then(() => self.clients.claim())));
self.addEventListener('fetch', event => { if (event.request.method !== 'GET') return; const url = new URL(event.request.url); if (url.origin !== self.location.origin || !url.pathname.startsWith(new URL('./', self.location.href).pathname)) return; if (event.request.mode === 'navigate') { event.respondWith(fetch(event.request).catch(() => caches.match('./index.html'))); return; } event.respondWith(caches.match(event.request).then(cached => cached || fetch(event.request))); });
